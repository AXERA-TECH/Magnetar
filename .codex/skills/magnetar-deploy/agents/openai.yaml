interface:
  display_name: "Magnetar Deploy"
  short_description: "Pulsar2 AXMODEL workflow and packaging"
  default_prompt: "Use $magnetar-deploy to deploy repo=[path or git URL] to AX650 with task_dir=[work directory], then simulate, run on board, and package demos if validation passes."

policy:
  allow_implicit_invocation: true
