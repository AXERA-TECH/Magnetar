# Magnetar

Pulsar2 Agent, 使用Claude工作流实现，目标旨在帮助用户实现 从开源或自研模型 -> ONNX模型 --> Pulsar2编译 --> AXMODEL --> 仿真验证 --> 板端运行 的流程，解放双手。

# 快速开始

In Claude console:
```
magnetar repo=[本地路径或git repo] task_dir=[工作目录, 默认为claude的临时目录]
```
